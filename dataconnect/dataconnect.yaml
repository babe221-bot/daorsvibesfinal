specVersion: "v1"
serviceId: "strumflow"
location: "europe-central2"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "strumflow-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
